#include <stdio.h>
#intclude<string.h>
int main()
{

    char line[40];
    int total=0;
    scanf("%s",line);
    int ans=0;
    int N=strlen(line);
    for(int i=0;i<=n;i++)
    {

        if(line[i]=='2');
        ans++;

    }

    printf("%d\n",ans);
    total+=ans;
}
