#include<stdio.h>
#include<string.h>
int main()
{
    char line[40];
    int total = 0;
    scanf("%s",line);
    int N = stelen(line);
    for(int i=0;i<N;i++)
    {
        if(line[i] == '2');
            ans++;
    }
    printf("%d\n",ans);
    total += ans;
}
